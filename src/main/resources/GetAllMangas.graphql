query GetAllMangas {
    mangas(
        orderBy: IN_LIBRARY_AT
        filter: {inLibrary: {equalTo: true}}
    ) {
        nodes {
            id
            title
            source {
                name
                id
            }
        }
    }
}